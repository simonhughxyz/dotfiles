---
- name: Create the '$HOME/.config/dunst' directory if not exists
  ansible.builtin.file:
    path: "{{ home }}/.config/dunst"
    mode: u=rwx,g=,o=
    state: directory

- name: Copy dunst files
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ home }}/.config/dunst/{{ item }}"
    mode: u=rw,g=,o=
    force: yes
  loop:
    - dunstrc
