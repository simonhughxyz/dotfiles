---
- name: Create the '$HOME/.config/fontconfig' directory if not exists
  ansible.builtin.file:
    path: "{{ home }}/.config/fontconfig"
    mode: u=rwx,g=,o=
    state: directory

- name: Copy fontconfig files
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ home }}/.config/fontconfig/{{ item }}"
    mode: u=rwx,g=,o=
    force: yes
  loop:
    - fonts.conf
