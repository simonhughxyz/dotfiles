---
- name: Create the '$HOME/.config/starship' directory if not exists
  ansible.builtin.file:
    path: "{{ home }}/.config/starship"
    mode: u=rwx,g=,o=
    state: directory

- name: Copy zathura files
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ home }}/.config/starship/{{ item }}"
    mode: u=rwx,g=,o=
    force: yes
  loop:
    - starship.toml
